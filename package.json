{
  "name": "babel-of-bug",
  "version": "1.0.0",
  "description": "Example of of iterator bug",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && node dist/index"
  },
  "author": "Richard van der Dys",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0"
  }
}
